# Local development only. Copy to .env for Docker Compose; .env is ignored by Git.
# Export matching variables in your shell/IDE when running Spring Boot on the host.
KAFKA_PORT=9092
# Stable KRaft cluster ID, not a secret. Keep it unchanged while reusing kafka-data.
KAFKA_CLUSTER_ID=MkU3OEVBNTcwNTJENDM2Qk
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=payments
POSTGRES_USER=payments
POSTGRES_PASSWORD=payments_dev_only
